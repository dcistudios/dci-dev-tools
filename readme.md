# DCI Dev Tools

A suite of utility tools for DCI developers.

## Quick Start
1. `npm install`
2. `npm start`
3. Open `http://localhost:3000`

## Docker
```bash
docker build -t dci-dev-tools .
docker run -p 3000:3000 dci-dev-tools
